/*using UnityEngine;

public class UpgradeShopController : MonoBehaviour
{
    // Referências para os botões da nave A
    public UpgradeButton[] shipAButtons;

    // Referências para os botões da nave B
    public UpgradeButton[] shipBButtons;

    // Referências para os botões de upgrades globais
    public UpgradeButton[] globalButtons;

    private void Start()
    {
        // Configuração inicial dos botões
        InitializeButtons();
    }

    private void InitializeButtons()
    {
        // Configurações iniciais dos botões da nave A
        for (int i = 0; i < shipAButtons.Length; i++)
        {
            // Defina as configurações do botão da nave A
            // Exemplo: shipAButtons[i].SetupButton("Item Upgrade", 1, 3, 100);
        }

        // Configurações iniciais dos botões da nave B
        for (int i = 0; i < shipBButtons.Length; i++)
        {
            // Defina as configurações do botão da nave B
            // Exemplo: shipBButtons[i].SetupButton("Item Upgrade", 1, 4, 150);
        }

        // Configurações iniciais dos botões de upgrades globais
        for (int i = 0; i < globalButtons.Length; i++)
        {
            // Defina as configurações do botão de upgrade global
            // Exemplo: globalButtons[i].SetupButton("Item Upgrade", 1, 5, 200);
        }
    }
}
*/